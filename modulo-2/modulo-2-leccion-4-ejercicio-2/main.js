"use strict";
function average(a, b, c, d) {
  const result = (a + b + c + d) / 4;

  return result;
}

const averageResult = average(5, 4, 3, 5); //averageResult vale 4.25
console.log(averageResult);
